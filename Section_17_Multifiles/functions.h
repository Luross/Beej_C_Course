#ifndef FUNCTIOns_H //to avoid infinite loops, we add those # at the beginning of every headers
#define FUNCTIONS_H // that makes it that if a file alreayd has functions.h defined, it will not define it again
int add(int, int);

#endif
